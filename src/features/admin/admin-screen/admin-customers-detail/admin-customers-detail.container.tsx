import { AdminCustomersDetailScreen } from "./admin-customers-detail.screen";

export const AdminCustomersDetailContainer = ({ navigation, route }: any) => {
  return <AdminCustomersDetailScreen navigation={navigation} route={route} />;
};
