import { ProfileDetailScreen } from "./profile-detail.screen";

export const ProfileDetailContainer = ({ route, navigation }: any) => {
  return <ProfileDetailScreen route={route} navigation={navigation} />;
};
